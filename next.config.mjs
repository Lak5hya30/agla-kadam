/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Low-bandwidth: produce a lean standalone-friendly build.
  poweredByHeader: false,
};

export default nextConfig;
